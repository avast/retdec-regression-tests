from regression_tests import *

#class Test(Test):
	#settings = TestSettings(
		#input='mips-malware-29ce',
		#args='-k --backend-no-opts'
	#)

	## TODO: there is plenty of strings, but we do not discover any of them.
	## However, this empty test forces to check that decompilation was successful.
	##
	#def test_check_for_strings(self):
		#assert self.out_c.contains(r'')
